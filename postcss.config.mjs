/** @type {import('next').NextConfig} */
const nextConfig = {

    images: {
    domains: ['upload.wikimedia.org', 'www.himalayawellness.in','localhost','192.168.29.194'],
  },
};

export default nextConfig;

